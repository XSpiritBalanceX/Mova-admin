import { Box } from "@mui/material";
import { translate } from "@i18n";
import "./PasswordRecovery.scss";

const EmailForm = () => {
  const { t } = translate("translate", { keyPrefix: "forgotPasswordPage" });
  return <Box>EmailForm</Box>;
};

export default EmailForm;
